<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Aadhaar_Pattern</fullName>
    <active>true</active>
    <description>QA BUG-0469: Primary Applicant Aadhaar number must be exactly 12 digits. Only fires when the value is being added or changed, so existing records are not blocked.</description>
    <errorConditionFormula>AND(
  OR(ISNEW(), ISCHANGED(Aadhaar__c)),
  NOT(ISBLANK(Aadhaar__c)),
  NOT(REGEX(Aadhaar__c, &quot;[0-9]{12}&quot;))
)</errorConditionFormula>
    <errorDisplayField>Aadhaar__c</errorDisplayField>
    <errorMessage>Aadhaar number must be exactly 12 digits.</errorMessage>
</ValidationRule>
