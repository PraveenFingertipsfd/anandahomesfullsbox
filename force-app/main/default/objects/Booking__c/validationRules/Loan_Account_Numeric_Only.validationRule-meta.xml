<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Loan_Account_Numeric_Only</fullName>
    <active>true</active>
    <description>QA BUG-0468: Loan Account Number must contain numeric values only. Only fires when the value is being added or changed, so existing records are not blocked.</description>
    <errorConditionFormula>AND(
  OR(ISNEW(), ISCHANGED(Loan_Account_Number__c)),
  NOT(ISBLANK(Loan_Account_Number__c)),
  NOT(REGEX(Loan_Account_Number__c, &quot;[0-9]+&quot;))
)</errorConditionFormula>
    <errorDisplayField>Loan_Account_Number__c</errorDisplayField>
    <errorMessage>Loan Account Number can contain numeric values only.</errorMessage>
</ValidationRule>
